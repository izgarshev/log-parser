package parser.entity;

public class Document extends Entity {
    public Document() {
    }

    public Document(String typeId, long id, int versionId) {
        super(typeId, id, versionId);
    }
}
